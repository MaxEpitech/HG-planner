# Configuration .htaccess pour Highland Games (si vous utilisez mod_rewrite)
# Placez ce fichier dans le répertoire public de votre application
# Note: Cette approche est moins recommandée que la configuration VirtualHost
# Utilisez plutôt apache2.conf.example avec un VirtualHost

# Activer le proxy
RewriteEngine On

# Proxy vers l'application Next.js
RewriteCond %{REQUEST_URI} !^/\.well-known/
RewriteRule ^(.*)$ http://localhost:3000/$1 [P,L]

# Headers pour le proxy
ProxyPreserveHost On
RequestHeader set X-Forwarded-Proto "http"
RequestHeader set X-Forwarded-Port "80"

# Taille maximale des uploads (10MB)
php_value upload_max_filesize 10M
php_value post_max_size 10M

